import {
    HdbChannelView,
    ActiveListView
} from './index';

export const hdb_views = [
    HdbChannelView,
    ActiveListView
];
export const hdb_view_map = {
    'hdb-channel': HdbChannelView,
    'active-list': ActiveListView
};